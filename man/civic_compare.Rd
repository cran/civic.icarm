% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/civic_scorecard.R
\name{civic_compare}
\alias{civic_compare}
\title{Compare multiple civic_models on a shared test set}
\usage{
civic_compare(
  models,
  test_data,
  outcome,
  protected = NULL,
  positive = NULL,
  threshold = 0.5
)
}
\arguments{
\item{models}{A **named** list of `civic_model` objects,
e.g. `list(CART = m1, Logistic = m2)`.}

\item{test_data}{A data frame used for all evaluations.}

\item{outcome}{Character. Outcome column name.}

\item{protected}{Character. Protected attribute column (optional).
Pass `NULL` to skip fairness metrics.}

\item{positive}{Positive class level (binary only).}

\item{threshold}{Decision threshold (binary only, default `0.5`).}
}
\value{
A tibble of class `civic_comparison`.
}
\description{
Evaluates a named list of `civic_model` objects on a common test
set and returns a tidy comparison table of performance, fairness,
and interpretability. All models must have the same task type.
}
\examples{
splits <- civic_split(iris, stratify = "Species")
m1 <- civic_fit(Species ~ ., splits$train, model = "cart")
m2 <- civic_fit(Species ~ ., splits$train, model = "multinomial")
cmp <- civic_compare(list(CART = m1, Multinomial = m2),
                     splits$test, outcome = "Species")
civic_plot_comparison(cmp)
}
