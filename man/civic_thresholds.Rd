% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/civic_data_utils.R
\name{civic_thresholds}
\alias{civic_thresholds}
\title{Threshold sweep for binary classification}
\usage{
civic_thresholds(
  y_true,
  y_prob,
  positive = NULL,
  thresholds = seq(0.1, 0.9, by = 0.05)
)
}
\arguments{
\item{y_true}{Factor of true class labels.}

\item{y_prob}{Numeric vector of predicted probabilities for the positive class.}

\item{positive}{Character. Positive class level.}

\item{thresholds}{Numeric vector of thresholds to evaluate.
Default: `seq(0.1, 0.9, by = 0.05)`.}
}
\value{
A tibble with one row per threshold and columns:
  `threshold`, `accuracy`, `balanced_acc`, `precision`,
  `recall`, `specificity`, `f1`, `rate_positive`.
}
\description{
Computes performance metrics across a grid of decision thresholds.
Essential for understanding the accuracy-vs-fairness tradeoffs that
arise when choosing a classification cutoff — a DataCitizen-Pro
democratic judgment teaching tool.
}
\examples{
y <- factor(sample(c("yes","no"), 200, replace = TRUE))
p <- runif(200)
thr <- civic_thresholds(y, p, positive = "yes")
civic_plot_thresholds(thr)
}
